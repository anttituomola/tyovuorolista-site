---
import Button from '../common/Button.astro'
import { Image } from 'astro:assets'
import tyovuorolista_hero from '../../assets/tyovuorolista_hero.png'
import RegisterButton from '../common/RegisterButton.astro'
---

<div class="hero">
    <h1>PK-yrityksen työvuorolista nopeasti ja helposti</h1>
    <div class="heroImage">
        <Image src={tyovuorolista_hero} alt="Työvuorolista" width={600} loading='eager'/>
    </div>
    <h3>Rekisteröidy, tee työvuorolista ja julkaise se alta viiden minuutin!</h3>
    <span class="buttons">
        <RegisterButton />
        <span style="color: var(--harmaa);">TAI</span>
        <Button text="Kokeile demoa" backgroundColor="var(--ruskeaharmaa)" href="https://app.tyovuorolista.fi/demo" target="_blank" />
    </span>
</div>

<style>
    .hero {
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        text-wrap: balance;
        padding: var(--space-xl) var(--space-base);
        gap: var(--space-xl);
    }
    .heroImage {
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
        border-radius: 8px;
        overflow: hidden;
        margin: var(--space-lg) 0;
    }
    h1 {
        font-size: var(--text-5xl);
        font-weight: 600;
        margin-bottom: var(--space-base);
        max-width: 90%;
    }
    h3 {
        font-size: var(--text-xl);
        font-weight: 400;
        max-width: 85%;
        margin-bottom: var(--space-lg);
        color: var(--harmaa);
    }
    .buttons {
        display: flex;
        align-items: center;
        gap: var(--space-base);
        flex-wrap: wrap;
        justify-content: center;
    }
    .buttons span {
        font-size: var(--text-sm);
        font-weight: 500;
    }
    @media (max-width: 768px) {
        .hero {
            gap: var(--space-lg);
            padding: var(--space-lg) var(--space-base);
        }
        h1 {
            font-size: var(--text-4xl);
            max-width: 95%;
        }
        h3 {
            font-size: var(--text-lg);
            max-width: 90%;
        }
        .heroImage img {
            width: 90%;
            height: auto;
        }
        .buttons {
            flex-direction: column;
            gap: var(--space-sm);
        }
    }
</style>
