---
import Button from '../common/Button.astro'
import { Image } from 'astro:assets';
import tyovuorolista_hero from '../../assets/tyovuorolista_hero.png'
---

<div class="hero">
    <h1>Ravintolan työvuorolista nopeasti ja helposti</h1>
    <div class="heroImage">
        <Image src={tyovuorolista_hero} alt="Työvuorolista" width={600} />
    </div>
    <h3>Rekisteröidy, tee työvuorolista ja julkaise se alta viiden minuutin!</h3>
    <span class="buttons">
        <Button text="Rekisteröidy" backgroundColor="var(--hiekka)" /> 
        <span style="color: var(--harmaa);">TAI</span>
        <Button text="Ota yhteyttä" backgroundColor="var(--ruskeaharmaa)" href="./yhteystiedot" />
    </span> 
</div>

<style>
    .hero {
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        text-wrap: balance;
    }
    .heroImage {
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }
    h1 {
        font-size: 5rem;
    }
    h3 {
        font-size: 30px;
        max-width: 80%;
    }
    @media (max-width: 640px) {
        h1 {
            font-size: 3rem;
        }
        .heroImage img {
            width: 80%;
            height: auto;
        }
        .buttons {
            display: flex;
            flex-direction: column;
        }
        @media (max-width: 640px) {
            .buttons {
                gap: 0.6rem;
            }
        }

    }
</style>