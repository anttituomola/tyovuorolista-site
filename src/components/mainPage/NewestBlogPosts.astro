---
const allPosts = await Astro.glob('../../pages/posts/*.md')
const fourMostRecentPosts = allPosts.slice(0, 4)
import BlogPostCard from '../common/BlogPostCard.astro'
---

<div class="wrapper">
    <h1 style="text-align: center;">Uusimmat tekstit ravintolan työvuorosuunnittelusta</h1>
    <div class="blogPosts">
        {fourMostRecentPosts.map((post) => <BlogPostCard post={post} />)}
    </div>
    <a href="/blogitekstit"><h3>Katso kaikki tekstit</h3></a>
</div>

<style>
    .blogPosts {
        display: flex;
        flex-direction: row;
        justify-content: space-between;
        flex-wrap: wrap;
        width: 100%;
    }
</style>