---
const { title, pubDate, image, description, tags } = Astro.props.post.frontmatter
const { url } = Astro.props.post
console.log('🚀 ~ file: BlogPostCard.astro:3 ~ title:', title)
console.log(Astro.props)
---

<div class="blogPostCard">
    <a href={url}>
        <div>
            <h1>{title}</h1>
            <p>{description}</p>
            <p>{pubDate}</p>
            <p>{tags}</p>
        </div>
    </a>
</div>

    <style>
        .blogPostCard {
            border: 1px solid var(--hiekka);
            padding: 10px;
            margin: 10px;
        }
        .blogPostCard a {
        text-decoration: none;
        color: var(--musta);
    }
    </style></a
>
